import {Observer} from "../Abstract/Observer";

export class LogicService extends Observer {
    // emailAdmin: string | null = "em000320@g.bstu.by"
    emailAdmin: string | null = "sergu9848@gmail.com"
}